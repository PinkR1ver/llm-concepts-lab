# 2026-04-10

## Project bootstrap

- Created project: `llm-concepts-lab`
- Purpose: build an LLM concepts learning lab with runnable scripts and visual dashboards
- Stack chosen: Next.js + Tailwind + shadcn/ui frontend, Python backend
- Python environment is project-local conda env at `./.conda`

## User preferences

- Jude wants concepts grounded in code, not only verbal explanation
- Jude prefers a more advanced frontend stack over Streamlit
- Jude likes vibe-coding projects to include a project-local `.agents/` folder
- Jude suggested using `.agents/AGENTS.md` plus a local `memory/` folder
- Jude noted project-local skills can also live in `.agents/skills/`

## Current implementation status

- Frontend scaffold exists in `frontend/`
- Embedding Playground first version is implemented
- Page currently explains text -> tokens -> token ids -> embedding lookup -> one-hot equivalence
- Design quality is acceptable for a prototype but needs stronger visual/system design
- Durable project architecture memory should live in stable files, not only dated session logs
- `Impeccable` is a candidate project-local skill for improving design quality and frontend craft

## Project-local skills

- Attempted `npx skills add pbakaus/impeccable`
- Network path to GitHub works with proxy, but the installer flow was unreliable in this environment
- Installed manually by cloning the upstream repo and copying `.agents/skills/` into the project
- Project now has local Impeccable-related skills under `.agents/skills/`

## Memory process

- Project rules should require memory updates after each meaningful implementation step
- Session logs are not enough; stable architecture memory must be maintained as first-class project context

## Visual/app-shell progress

- Added `.impeccable.md` with explicit design context for the project
- Reworked the frontend into a real app shell with a persistent sidebar
- Sidebar now establishes long-term module navigation for future concept pages
- Embedding page was redesigned to feel more like a digital exhibition + refined developer tool
- Added dark/light theme toggle and stronger information hierarchy
- Added token-focused interaction so selecting a token highlights its ids and vector rows
- Frontend lint/build pass successfully after the redesign

## Next useful improvements

1. Add dedicated routes/pages for future concept modules rather than placeholder sidebar entries
2. Add low-dimensional vector visualization for embeddings
3. Add richer motion and progressive disclosure where it helps learning
4. Add a Python backend endpoint for generated examples when needed
5. Prepare deployment once a Git remote / GitHub Pages target exists
